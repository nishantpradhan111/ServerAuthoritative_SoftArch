# k6 Benchmarks

This folder contains k6 scripts for reproducible load benchmarking.

- `http-room-lifecycle.js`: Auth + room lifecycle benchmark over HTTP.

Run from repository root:

```powershell
k6 run benchmarks/k6/http-room-lifecycle.js
```
